# Multitype Client

## Intro

Client-side application for multiplayer typing game "Multitype", created using React + TypeScript + Vite.

## Setup

To run this web app on your machine, you will need:

- Node.js
- npm

Ensure your working directory is the root of this repository.

To start the web app, run the following commands:

```
npm install
npm run dev
```